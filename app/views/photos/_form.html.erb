<%= form_for @photo do |f| %>
    <div class="form-group row">
      <%= f.label :title, 'Título:', class: 'col-sm-1 col-form-label' %>
      <div class="col-sm-11">
        <%= f.text_field :title, class: 'form-control' %>
      </div>
    </div>
    <div class="form-group row">
      <%= f.label :date, 'Data:', class: 'col-sm-1 col-form-label' %>
      <div class="col-sm-11">
        <%= f.text_field :date, class: 'form-control' %>
      </div>
    </div>
    <div class="form-group row">
      <%= f.label :location, 'Local:', class: 'col-sm-1 col-form-label' %>
      <div class="col-sm-11">
        <%= f.text_field :location, class: 'form-control' %>
      </div>
    </div>
    <div class="form-group row">
      <%= f.label :photo, 'Foto:', class: 'col-sm-1 col-form-label' %>
      <%= f.file_field :photo, class: 'form-control' %>
    </div>

    <%= f.submit 'Enviar', class: 'btn btn-default' %>
<% end %>